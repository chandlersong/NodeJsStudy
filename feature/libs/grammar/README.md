- [variable scope](./scope.js)
    - variable has been changed: the function use the variable out of it's scope, so it will change
    - variable not changed: use Closure to make the variable not change 